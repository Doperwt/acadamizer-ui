import AskQuestion from './AskQuestion'
import React from 'react'

describe('askQuestion functions',()=>{

  // it('yields random number',() =>{
  //   let number = 0
  //   number = AskQuestion.randomStudent(5)
  //   expect(number).toNotBe(0)
  // })
})
